# 工程决策日志

## 项目信息

- **项目名称**: ICS 课表导入
- **创建时间**: 2026-09-03

## 决策记录

### DEC-001: 技术栈选择

**日期**: 2026-09-03
**状态**: 已确认
**决策者**: AI 助手

**背景**:
项目需求是一个静态网页应用，需要部署在 GitHub Pages 上，无需后端服务。

**决策**:
采用纯 HTML + CSS + JavaScript 技术栈，不引入任何框架或构建工具。

**理由**:
1. GitHub Pages 原生支持静态文件托管
2. 无需复杂的构建流程
3. 降低项目复杂度
4. 便于维护和调试
5. 符合 `readme.md` 中的技术栈定义

**影响**:
- 开发效率：简单直接，无需配置构建工具
- 维护成本：低，文件结构清晰
- 部署方式：直接推送到 GitHub Pages

---

### DEC-002: 学校列表数据来源

**日期**: 2026-09-03
**状态**: 已确认
**决策者**: AI 助手

**背景**:
需要动态获取学校列表，有两种方案：
1. 本地维护 `schools/config.json`
2. 远程获取 `rules_index.json`

**决策**:
采用远程获取方式，从 `https://raw.githubusercontent.com/cjz-wr/PlanCraftDownload/main/rules_index.json` 实时获取。

**理由**:
1. `readme.md` 明确说明"学校列表不再通过本地 `schools/config.json` 维护"
2. 远程获取可以实时更新学校列表
3. 无需手动维护本地配置文件
4. 便于后续扩展新学校

**影响**:
- 依赖外部数据源的可用性
- 需要处理网络请求失败的情况
- 需要实现降级方案（如缓存机制）

---

### DEC-003: 脚本加载策略

**日期**: 2026-09-03
**状态**: 已确认
**决策者**: AI 助手

**背景**:
每个学校有对应的课表提取脚本，需要动态加载并执行。

**决策**:
采用动态 `<script>` 标签加载，每次从远程仓库获取最新脚本。

**理由**:
1. `readme.md` 明确要求"每次从远程仓库获取最新课表解析脚本"
2. 动态加载可以获取最新版本
3. 无需打包到本地

**影响**:
- 每次使用都需要网络请求
- 需要处理脚本加载失败的情况
- 需要考虑跨域问题（CORS）

---

### DEC-004: 文件上传服务

**日期**: 2026-09-03
**状态**: 已确认
**决策者**: AI 助手

**背景**:
生成 ICS 文件后，用户需要分享链接。

**决策**:
使用 tmpfiles.org API 进行文件上传和分享。

**理由**:
1. `readme.md` 明确指定使用 tmpfiles.org
2. 免费服务，无需注册
3. API 简单易用
4. 支持自定义过期时间

**影响**:
- 依赖第三方服务的可用性
- 文件有大小限制（100 MB）
- 文件有过期时间（60-172800 秒）

---

### DEC-005: ICS 文件格式

**日期**: 2026-09-03
**状态**: 已确认
**决策者**: AI 助手

**背景**:
需要生成标准的日历文件供用户导入。

**决策**:
生成符合 RFC 5545 标准的 `.ics` 文件。

**理由**:
1. iCalendar 是行业标准格式
2. 支持主流日历应用（Apple 日历、Google 日历、Outlook）
3. `readme.md` 明确要求

**影响**:
- 需要严格遵循 RFC 5545 规范
- 需要处理时区问题
- 需要正确计算课程时间偏移

---

### DEC-006: WebView 交互方式

**日期**: 2026-09-03
**状态**: 已确认
**决策者**: AI 助手

**背景**:
需要打开学校教务系统的登录页面，用户登录后才能获取课表数据。

**决策**:
使用 `window.open()` 打开学校登录页面，通过动态脚本加载方式获取数据。

**理由**:
1. `readme.md` 中的 `webview_config` 配置了 `login_url`
2. 需要用户先登录教务系统
3. 学校教务系统设置了 `X-Frame-Options: deny`，禁止 iframe 嵌入
4. 纯静态网页应用无法直接控制 WebView

**技术限制**:
- 学校教务系统设置了 `X-Frame-Options: deny`，无法嵌入到 iframe 中
- 这是安全限制，防止点击劫持攻击
- 只能使用 `window.open()` 打开新窗口

**影响**:
- 用户需要在新窗口中登录
- 用户登录后需要返回原页面点击"智能一键识别课表"
- 使用动态脚本加载方式获取课表数据

---

### DEC-007: 识别机制改为“内嵌浏览器 + 本地沙箱导入”

**日期**: 2026-09-03
**状态**: 已确认

**背景**:
原“在主页动态加载学校脚本”的方式必然失败（学校脚本依赖课表页 DOM，且受 CORS/同源限制），需要不依赖用户脚本即可识别。

**决策**:
1. 登录：SSO 等禁止内嵌的登录页用外部浏览器窗口登录（仅登录），登录后自动跳转。
2. 展示：把登录后跳转到的课表网站载入主页内嵌浏览器（iframe）。
3. 识别：优先读取同源内嵌页面；跨域受限时引导导入课表 HTML，在本地同源沙箱（srcdoc）中执行该校官方提取脚本 `extractSchedule()` 解析（数据不上传）。
4. 用户脚本（Tampermonkey）降级为可选兼容能力（`postMessage` 回传仍被支持）。

**理由**:
- 纯静态页 + 无扩展无法注入跨域 iframe（同源策略）。
- srcdoc 沙箱与主页同源，可在其中运行学校脚本且能正常解析课表 DOM。
- 实现“不装脚本也能识别”，同时保留精确的学校级解析脚本。